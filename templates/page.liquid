{%- layout 'theme' -%}

<section class="container">
  <div class="section-kicker">{{ page.title }}</div>
  <h1 class="section-title">{{ page.title }}</h1>
  <div class="page-content">
    {{ page.content }}
  </div>
</section>
